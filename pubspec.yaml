name: jannatul_portfolio
description: Jannatul Nayeem — Developer Portfolio
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  url_launcher: ^6.2.4
  animated_text_kit: ^4.2.2
  flutter_animate: ^4.5.0
  smooth_page_indicator: ^1.1.0
  lottie: ^3.1.0
  flutter_svg: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  gh_pages: ^0.1.1

flutter:
  uses-material-design: true
  assets:
    - images/
